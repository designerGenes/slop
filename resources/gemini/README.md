# Readme

Gemini is a unique case because it has a tight character limit for each instruction.  However, you can add an arbitrary number of instructions, so it is possible to split the full instruction set into multiple smaller instructions.  They will all be combined together in Gemini's memory and obeyed systemically.