---
name: "Tiny Math"
description: "Compute a small integer expression with a minimal response"
argument-hint: "Optional integer n; computes n^2 + 1"
agent: "agent"
---
For the integer `n` in the request, compute `n^2 + 1`. If no integer is provided, use `7`.
Reply exactly in this format and nothing else:

`result=<number>`
